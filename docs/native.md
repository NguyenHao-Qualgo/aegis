# Native Bundle Creation Guide

The detailed native / Yocto bundle creation guide has moved to [build.md](build.md).

Use [build.md](build.md) for:

- the Yocto recipe example
- `IMAGE_DEPENDS`, `ESP_ARCHIVE`, and `UPDATE_IMAGES`
- `AEGIS_PRIVATE_KEY`, `AEGIS_PUBLIC_KEY`, and `AEGIS_AES_FILE`
- how to collect the generated `.swu`
- which keys must be provisioned on the target

For target runtime behavior, see [target.md](target.md).
